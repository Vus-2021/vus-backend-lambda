type Mutation {
    updateApplyUser(
        userId: String!
        month: String!
        state: String!
        isCancellation: Boolean!
    ): updateApplyUserResponse
}

type updateApplyUserResponse implements Response {
    success: Boolean!
    message: String!
    code: Int!
}
