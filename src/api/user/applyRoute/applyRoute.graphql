type Mutation {
    applyRoute(
        route: String!
        month: String!
        partitionKey: String!
        userId: String
        detailPartitionKey: String!
    ): applyRouteResponse
}

type applyRouteResponse implements Response {
    success: Boolean!
    message: String!
    code: Int
}
