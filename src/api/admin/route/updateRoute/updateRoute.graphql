type Mutation {
    updateRoute(
        partitionKey: String!
        route: String
        busNumber: String
        limitCount: Int
        driver: driver
        imageUrl: String
    ): updateRouteResponse
}

type updateRouteResponse implements Response {
    success: Boolean!
    message: String!
    code: Int
}
