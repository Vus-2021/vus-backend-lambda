type Mutation {
    createRouteDetail(
        route: String!
        location: String!
        lat: Float!
        long: Float!
        boardingTime: String!
        imageUrl: String!
    ): createRouteDetailResponse
}

type createRouteDetailResponse implements Response {
    success: Boolean!
    message: String!
    code: Int
}
